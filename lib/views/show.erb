
<html>
  <head>
    <title>PAGE POTIN</title>
  </head>
  <body>
    
    <h1>Voici la page du potin n°<%= params['id'] %></h1>
    <p>Son auteur est : <%=Gossip.find(params['id']).author %></p>
    <p>Le contenu du potin est : <%= Gossip.find(params['id']).content %></p>
    <a href="/gossips/<%=params['id']%>/edit">Lien pour editer les potins</a><br>
    <a href="/">Lien pour revenir à la page des potins</a>

  </body>
</html>







