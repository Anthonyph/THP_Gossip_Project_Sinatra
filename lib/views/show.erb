
<html>
  <head>
    <title>PAGE POTIN</title>
  </head>
  <body>
    <h1>Voici la page du potin n°<%= params['id'] %></h1>
    <p><b>Son auteur est : <%=Gossip.find(params['id']).author %></b></p>
    <p><b>Le contenu du potin est : <%= Gossip.find(params['id']).content %></b></p>
  </body>
</html>

<a href="/">Lien pour revenir à la page des potins</a>





